import start from "@/action/start";
import stop from "@/action/stop";
import pause from "@/action/pause";
import resume from "@/action/resume";
import fail from "@/action/fail";

export default {
    start,
    stop,
    pause,
    resume,
    fail,
};
